<%= form_for(@company) do |f| %>
    <% if @company.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@company.errors.count, "error") %> prohibited this topic from being saved:</h2>

          <ul>
            <% @company.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label :detail %><br>
      <%= f.text_field :detail %>
    </div>
    <%= select(:movie_id, options_for_select(movies_array.map{ |movie| [" #{movie.id} .) #{movie.title} ",movie.id] }))%>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
